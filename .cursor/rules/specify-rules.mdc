---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# clearui Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-07

## Active Technologies
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (standard C library only) (005-node-last-child)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None for default build; font metrics via vendored single-file C (e.g. stb_truetype) (007-font-atlas)
- N/A (font data can be embedded or loaded once at init) (007-font-atlas)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (stdlib malloc/free for dynamic buffer; optional static allocation path) (009-dynamic-draw-buffer)
- Draw command buffers owned by `cui_ctx`; allocated at create, freed at destroy (009-dynamic-draw-buffer)
- Draw buffers owned by `cui_ctx`; scaled_buf is one of them (010-move-scaled-buf-to-context)
- Declared tree in frame arena (reset each frame). Retained tree in context-owned storage (heap or persistent arena); diff allocates/frees retained nodes. (011-diff-engine)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (vendored `stb_truetype.h` for font metrics) (012-review-fixes)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (zero external dependencies; uses built-in stub platform + software RDI) (013-api-demo-example)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (stdlib only; optional vendored stb_truetype for font) (014-security-docs-overflow)
- N/A (no new persistence; CHANGELOG and docs are versioned in repo) (015-release-hygiene-0.2.0)
- Retained UI tree (context) holds cursor index per text input; buffer is caller-owned, content in app memory (016-text-input-keyboard-0.3.0)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (stdlib only; platform adapter for optional cursor_set) (017-scroll-mouse-0.4.0)
- N/A (scroll offset already in retained node; hover id in context) (017-scroll-mouse-0.4.0)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (stdlib only; existing stub platform/RDI for tests) (018-test-coverage-0.5.0)
- N/A (tests only; no new persistent state) (018-test-coverage-0.5.0)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (stdlib only) (019-theming-0.6.0)
- Theme stored in `cui_ctx` (struct copy or pointer to default); app-owned theme struct when set (019-theming-0.6.0)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + SDL3 for the SDL3 adapter (optional dependency; build when SDL3 available) or none for a minimal native adapter. Zero deps for “bring your own” documentation. (020-platform-backend-0-7-0)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + Vendored stb_truetype (existing in font atlas); optional stb_image_write or minimal PNG write for visual regression if not comparing raw buffer checksums. No new mandatory deps. (021-render-driver-0-8-0)
- N/A (framebuffer and glyph cache in RDI context; no persistent DB). (021-render-driver-0-8-0)
- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None for core; fuzz: libFuzzer or AFL (compiler/build option); leak: Valgrind or LeakSanitizer (CI). (022-robustness-limits-0-9-0)
- N/A (limits and error state in context or config). (022-robustness-limits-0-9-0)

- C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None (zero external dependencies) (002-fix-gitignore)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for C11 (`-std=c11 -Wall -Wextra -Wpedantic`)

## Code Style

C11 (`-std=c11 -Wall -Wextra -Wpedantic`): Follow standard conventions

## Recent Changes
- 022-robustness-limits-0-9-0: Added C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + None for core; fuzz: libFuzzer or AFL (compiler/build option); leak: Valgrind or LeakSanitizer (CI).
- 021-render-driver-0-8-0: Added C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + Vendored stb_truetype (existing in font atlas); optional stb_image_write or minimal PNG write for visual regression if not comparing raw buffer checksums. No new mandatory deps.
- 020-platform-backend-0-7-0: Added C11 (`-std=c11 -Wall -Wextra -Wpedantic`) + SDL3 for the SDL3 adapter (optional dependency; build when SDL3 available) or none for a minimal native adapter. Zero deps for “bring your own” documentation.


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
